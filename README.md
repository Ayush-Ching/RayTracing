# Ray Tracing (in a month....?)

Here, I follow the "RayTracing in One Weekend" Book series ([here](https://raytracing.github.io/)) to write a static ray tracer in C++ which outputs the image in `.ppm` format.

The current output looks like this :

![the output](/img/image.png "a beautiful chessboard indeed")